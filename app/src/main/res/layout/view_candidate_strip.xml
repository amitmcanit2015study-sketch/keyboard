<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="44dp"
    android:background="@color/candidate_bar_bg"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingStart="4dp"
    android:paddingEnd="4dp">

    <!-- 1. Top-left Language Switcher Pill Button (HN / EN) -->
    <LinearLayout
        android:id="@+id/btnLanguageToggle"
        android:layout_width="wrap_content"
        android:layout_height="32dp"
        android:layout_marginEnd="6dp"
        android:background="@drawable/bg_lang_badge"
        android:clickable="true"
        android:focusable="true"
        android:gravity="center"
        android:orientation="horizontal"
        android:paddingStart="8dp"
        android:paddingEnd="8dp">

        <ImageView
            android:layout_width="14dp"
            android:layout_height="14dp"
            android:layout_marginEnd="4dp"
            android:src="@drawable/ic_globe"
            app:tint="@color/lang_pill_text" />

        <TextView
            android:id="@+id/tvActiveLang"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="HN"
            android:textColor="@color/lang_pill_text"
            android:textSize="12.5sp"
            android:textStyle="bold" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="2dp"
            android:layout_marginEnd="2dp"
            android:text="|"
            android:textColor="@color/lang_pill_text"
            android:textSize="10sp"
            android:alpha="0.6" />

        <TextView
            android:id="@+id/tvAltLang"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="EN"
            android:textColor="@color/lang_pill_text"
            android:textSize="10.5sp"
            android:alpha="0.7" />
    </LinearLayout>

    <View
        android:layout_width="1dp"
        android:layout_height="22dp"
        android:layout_marginEnd="6dp"
        android:background="@color/candidate_bar_border" />

    <!-- 2. Horizontal Candidates Strip (Suggestions) -->
    <HorizontalScrollView
        android:id="@+id/scrollSuggestions"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="1"
        android:fillViewport="true"
        android:scrollbars="none">

        <LinearLayout
            android:id="@+id/layoutSuggestionsContainer"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:gravity="center_vertical"
            android:orientation="horizontal" />
    </HorizontalScrollView>

    <View
        android:layout_width="1dp"
        android:layout_height="22dp"
        android:layout_marginStart="6dp"
        android:layout_marginEnd="4dp"
        android:background="@color/candidate_bar_border" />

    <!-- 3. Quick Settings Action -->
    <ImageButton
        android:id="@+id/btnQuickSettings"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:background="?android:attr/selectableItemBackgroundBorderless"
        android:contentDescription="Settings"
        android:padding="8dp"
        android:src="@drawable/ic_settings" />

</LinearLayout>
